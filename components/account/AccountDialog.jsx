import styles from "./styles/AccountDialog.module.scss";

function AccountDialog({ isAccountExpanded, clickHandler }) {
	return (
		<div id="accountDialog" className={styles.modal}>
			AccountDialog
		</div>
	);
}

export default AccountDialog;
