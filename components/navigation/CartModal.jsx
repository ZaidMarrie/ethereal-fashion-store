import styles from "@/styles/components/cartModal.module.scss";

function CartModal({ isCartExpanded, clickHandler }) {
	return (
		<div id="cartModal" className={styles.modal}>
			CartModal
		</div>
	);
}

export default CartModal;
