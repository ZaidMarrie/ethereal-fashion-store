import styles from "@/styles/components/AccountModal.module.scss";

function AccountModal({ isAccountExpanded, clickHandler }) {
	return (
		<div id="accountModal" className={styles.modal}>
			AccountModal
		</div>
	);
}

export default AccountModal;
